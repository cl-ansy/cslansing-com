define([
    'react'
    ], function (React) {

    var Footer = React.createClass({
        render: function() {
            return (
                <footer>
                    Footer
                </footer>
            );
        }
    });

    return Footer;
});
